# Seed 7711 checkpoint (480/480)

This immutable recovery point contains the complete seed-7711 shard:
120 B0, 120 B1, 120 B2, and 120 B3 persisted JSONL events.

Only `events.checkpoint.jsonl` is counted as raw event evidence. Its SHA-256 is
`8a27a2280f550887be0cf031f900ad1aec16a3441719c17683185c027315d6bc`.
The handoff validator found zero prompt mismatches and confirmed the frozen
notebook code hash, runner version, model pins, decoding contract, and
seed-3407-equivalent environment.

This checkpoint completes the second of five planned generation-seed shards.
The two complete shards contain 960 of 2,400 planned events and 240 of 600
paired B2/B3 tasks. They support two-seed diagnostics only; they do not support
a final campaign or tuned-model-improvement claim.
